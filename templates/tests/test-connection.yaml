apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hp-ams.fullname" . }}-test-connection"
  labels:
    {{- include "hp-ams.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hp-ams.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
